sftp:
  host: localhost
  port: -1
  username: user
  privateKey:
    file: classpath:META-INF/keys/sftp_rsa
    passphrase: password
  remote:
    directory: si.sftp.sample
    filter: .*.txt
  local:
    directory: local-dir

logging:
  leve:
    root: TRACE